# 安全边界

本版是离线个人工具，不是可信竞技积分、支付钱包或加密日记。用户可修改本地时钟或明文备份，因此不提供抗作弊保证。SHA-256 用于检查损坏，不验证作者身份。

发现导入导致数据丢失、非预期读取/上传、外键或重复结算问题时，先使用合成数据复现。不要在公开 Issue 上传真实备份、私人任务、签名文件、Token 或账号凭证。正式公开发布前，维护者需要设置并公布私密漏洞报告渠道。

依赖与 CI 的最终权限需在实际构建/仓库创建后审核。现有 GitHub 工作流默认 contents: read，不包含商店上传或公共发布动作。签名 workflow 仍需要维护者设置 production Environment 审批保护。
