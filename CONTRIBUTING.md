# 开发约定

改动先确认是产品规则、平台行为还是 UI 表现。纯规则必须可通过 core 测试，不把调用 Compose 或 Android 时间 API 当作领域依赖。

提交前运行领域测试、Android Lint 和对应仪器测试；报告未能运行的项目。修改数据库时更新版本、迁移和真实 schema。修改奖励、重复日程或导入协议时优先加失败用例与边界测试。

提交不得包含私人任务、手记、导出存档、日志中的用户内容、签名密钥或 Token。初始仓库默认私有，对外开源许可由维护者决定。

PR 需要说明变更、已执行测试、尚未执行测试、数据兼容性及截图来源。模拟图不能作为已运行界面的证据。
